{
  "name": "medassistai",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "bootstrap": "yarn install"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "nodemon": "^3.1.14",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.2.3"
  }
}
